# NLPL Compiler Dependencies
psutil>=5.9.0
tomli-w>=1.0.0  # TOML writer (stdlib tomllib handles reading on Python 3.11+)
cbor2>=5.4.0    # CBOR serialization (RFC 8949) for stdlib/serialization
lz4>=4.0.0      # LZ4 fast compression for stdlib/compression
zstandard>=0.21.0  # Zstandard (zstd) compression for stdlib/compression
