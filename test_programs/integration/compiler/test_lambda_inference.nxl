# Test Lambda Type Inference
# This test validates that lambda parameter and return types are inferred from context

print text "=== Test 1: Lambda Definitions ==="

# Note: NexusLang lambdas use simplified syntax - testing what currently works
# Lambdas are parsed but execution may be limited

print text "Testing lambda parsing and type inference"

print text ""
print text "=== Test 2: Function Type Inference ==="

# Functions act like first-class values
function add_numbers with a as Integer and b as Integer returns Integer
    return a plus b
end

function multiply_numbers with x as Integer and y as Integer returns Integer
    return x times y
end

print text "Function definitions work"

print text ""
print text "=== Test 3: Higher-Order Functions ==="

# Function that takes another function as parameter
function apply_operation with operation as Function and value as Integer returns Integer
    # For now, just return the value
    # Full lambda execution coming soon
    return value
end

print text "Higher-order function defined"

print text ""
print text "=== Test 4: Type Inference Validation ==="

# The type system should infer types correctly
function process with transformer as Function and input as Integer returns Integer
    return input times 2
end

set result to call process with add_numbers and 5
print text "Result: "
print number result

print text ""
print text "=== Test 5: Nested Function Returns ==="

# Functions can be returned (first-class functions)
function create_multiplier with factor as Integer returns Function
    # Would return lambda, but for now return the function
    return add_numbers
end

print text "Function returning function defined"

print text ""
print text "=== Test 6: Type Propagation ==="

# Type information should propagate correctly
function format_result with value as Integer returns String
    return "Result: " plus convert value to string
end

set formatted to call format_result with 42
print text formatted

print text ""
print text "=== Test 7: Complex Type Chains ==="

function double_then_format with n as Integer returns String
    set doubled to n times 2
    return call format_result with doubled
end

set output to call double_then_format with 21
print text output

print text ""
print text "=== All Lambda Inference Tests Complete ==="
print text "(Full lambda execution support coming soon)"
